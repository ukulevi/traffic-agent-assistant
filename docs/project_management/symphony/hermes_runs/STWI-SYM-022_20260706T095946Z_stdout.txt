I made a mistake just now. The prompt is explicit and operational: review the changed Symphony workflow artifacts under `docs/project_management/symphony/**`, run the exact checks, regenerate `board.md` and `status_report.md`, verify them, and write the final report file in-place — without committing or touching Linear. Instead of executing that, I tried to look up a skill bootstrap workflow and introduced an avoidable syntax error.

Next action should be: I do the remaining work directly in this repo. I will inspect the modified and untracked Symphony artifacts, group them into the coherent change set TRA-18 describes, run the exact checks, produce the timestamped final report under `docs/project_management/symphony/hermes_runs/`, and report only real pass/fail results.
