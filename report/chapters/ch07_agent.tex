\chapter{Tầng 4: LangGraph và Counterfactual Safety Loop}

\section{Vai trò}

Tầng 4 là workflow hỗ trợ quyết định, không phải actuator. LangGraph điều phối các node có tool allowlist; Celery chạy job nền và Redis lưu hàng đợi/progress. Mọi kết quả đều cần operator phê duyệt.

Counterfactual Safety Loop (CSL) lấy cảm hứng từ CF-VLA nhưng STWI không phải mô hình Vision--Language--Action end-to-end. Hệ thống áp dụng ý tưởng tự phản biện lên dữ liệu, mô phỏng và căn cứ pháp lý.

Mỗi vòng CSL đánh giá một candidate có kiểu và khác candidate trước. Refiner
MVP chỉ tăng 	exttt{green\_time\_ratio} khi failure duy nhất là V/C, trong khi
citation, uncertainty và OOD đều hợp lệ; surrogate sau đó chạy lại với candidate
mới. OOD, uncertainty cao, thiếu citation, input không hợp lệ hoặc lỗi dependency
dừng ngay. Vòng lặp tối đa ba candidate, fail closed và không phải optimizer hay
actuator tổng quát.

\section{State Machine}

\begin{enumerate}[leftmargin=1.5cm]
  \item Parse scenario và tạo \textit{IncidentVector}.
  \item Chạy baseline forecast và surrogate.
  \item Nếu OOD/uncertainty cao, trả \texttt{needs\_review}.
  \item Truy xuất Qdrant và chạy typed \textit{SimulationQuery}.
  \item Citation validator xác nhận hiệu lực và provision.
  \item Sinh candidate và chạy CSL tối đa ba vòng.
  \item Chỉ khi mọi policy pass mới trả \texttt{succeeded}.
\end{enumerate}

\begin{table}[H]
\centering
\caption{Quy tắc fail-closed}
\begin{tabular}{p{6cm} p{7cm}}
\toprule
\textbf{Điều kiện} & \textbf{Kết quả} \\
\midrule
Policy pass, đủ citation & \texttt{succeeded} + \texttt{recommended\_action} \\
Không hội tụ sau 3 vòng & \texttt{needs\_review} + candidate không executable \\
OOD hoặc uncertainty cao & \texttt{needs\_review} \\
Thiếu căn cứ còn hiệu lực & \texttt{needs\_review} hoặc \texttt{failed} \\
Vượt 180 giây & \texttt{expired} \\
Tool lỗi không phục hồi & \texttt{failed} \\
\bottomrule
\end{tabular}
\end{table}

Ngưỡng V/C mặc định 0,9 là policy cấu hình có version, không phải quy định pháp luật.

\section{API Job Bất đồng bộ}

\subsection{Ngữ cảnh Dashboard Production}

Trước khi tạo job, dashboard production gọi
\texttt{GET /api/v1/ui-context}. Endpoint read-only này ghép principal đã xác
thực với node allowlist và capability do provider phía server cấp; response có
\texttt{Cache-Control: no-store}. Client không được cung cấp identity, role,
node scope hay capability dùng cho authorization. Quyền
\texttt{record\_decision} còn được chặn theo role
\texttt{operator}/\texttt{admin}; không tồn tại quyền actuation.

Thiếu principal, role không phù hợp hoặc scope không khả dụng lần lượt trả lỗi
\texttt{AUTH\_PRINCIPAL\_REQUIRED}, \texttt{AUTH\_ROLE\_DENIED} hoặc
\texttt{UI\_CONTEXT\_UNAVAILABLE} kèm trace ID. Production không fallback demo
sau các lỗi này. Demo/test không đăng ký endpoint và chỉ 404 chính xác mới mở
đường kiểm tra OpenAPI provisional; payload malformed chuyển dashboard sang
static preview không mutating.

\subsection{Tạo Job}

\begin{lstlisting}[caption={Tạo What-if job},label={lst:create_job}]
POST /api/v1/what-if-jobs
{
  "tenant_id": "tenant-a",
  "scenario_time": "2026-06-21T08:00:00+07:00",
  "candidate_action": {
    "node_id": "node_00",
    "green_time_ratio": 0.70
  },
  "node_ids": ["node_00"],
  "scenario_query": "Danh gia can cu tai node_00",
  "horizons_minutes": [5, 10, 15, 30],
  "jurisdiction": "VN",
  "vc_threshold": 0.90
}

HTTP/1.1 202 Accepted
{
  "job_id": "uuid...",
  "status": "queued",
  "tenant_id": "tenant-a"
}
\end{lstlisting}

\subsection{Trạng thái và SSE}

\texttt{GET /api/v1/what-if-jobs/\{job\_id\}} trả snapshot.
\par\noindent
\texttt{GET /api/v1/what-if-jobs/\{job\_id\}/events} phát SSE gồm stage, iteration, progress, message và timestamp.

Status enum là \texttt{queued}, \texttt{running}, \texttt{succeeded}, \texttt{needs\_review}, \texttt{failed}, \texttt{expired}. SSE reconnect không được tạo execution mới.

\subsection{Kết quả}

\begin{lstlisting}[caption={Kết quả an toàn},label={lst:success_result}]
{
  "status": "succeeded",
  "recommended_action": {
    "node_id": "node_00",
    "green_time_ratio": 0.85,
    "executable": false,
    "automatic_actuation": false,
    "requires_operator_approval": true
  },
  "citations": [{
    "document_id": "sop-14",
    "provision": "Muc 3.2",
    "source_url": "https://internal.example/sop-14",
    "effective_from": "2026-01-01",
    "content_hash": "sha256:..."
  }],
  "safety_evaluation": {
    "policy_version": "csl-1.0",
    "iterations": 2,
    "converged": true
  },
  "trace_id": "tr_01J...",
  "latency_ms": 21400
}
\end{lstlisting}

\begin{lstlisting}[caption={Kết quả cần review},label={lst:review_result}]
{
  "status": "needs_review",
  "candidate_action": {
    "summary": "Phuong an thu nghiem: phan luong 20% sang B",
    "executable": false,
    "requires_operator_approval": true
  },
  "warnings": [{
    "code": "SAFETY_NOT_CONVERGED",
    "message": "Max V/C con 0.93 sau 3 vong"
  }],
  "citations": [],
  "trace_id": "tr_01J..."
}
\end{lstlisting}

\section{Observability và Nghiệm thu}

Mỗi transition ghi trace span, input/model/policy/corpus version, citations, số vòng lặp và operator decision. Retry chỉ dành cho tool idempotent và phải tôn trọng deadline chung.

\begin{itemize}
  \item Surrogate $P_{99}<500$ms trên benchmark profile.
  \item E2E $P_{95}\leq30$s; hard deadline/$P_{99}\leq180$s.
  \item \texttt{recommended\_action} chỉ xuất hiện ở \texttt{succeeded}.
  \item Mọi OOD, citation failure, policy failure và timeout đều có test fail-closed.
  \item Không có endpoint điều khiển thiết bị hiện trường.
\end{itemize}
